import ContentStudioClient from '@/app/dashboard/smi-agent/content-studio/components/ContentStudioClient';

export const metadata = {
  title: 'AI Content Studio - SMI Agent',
  description: 'One Workspace to Plan, Create, Optimize, and Repurpose High-Converting Property Content.',
};

export default function ContentStudioPage() {
  return <ContentStudioClient />;
}
